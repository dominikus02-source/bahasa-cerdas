// Exam result screen, mounted inside /arena. See ../page.tsx for why this exists.
// "Kembali" resolves to /arena/simulasi/{ukbi,tka} when mounted here.
export { default } from "@/app/(dashboard)/kompetisi/[paketId]/hasil/page";
