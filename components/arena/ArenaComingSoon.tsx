import { Clock3, Sparkles } from "lucide-react";
import type { GameDefinition } from "@/lib/arena/game-registry";
export default function ArenaComingSoon({ games }: { games: GameDefinition[] }) { if (games.length === 0) return null; return <section aria-labelledby="arena-coming-soon-heading"><div className="mb-4"><p className="text-[11px] font-black uppercase tracking-[0.18em] text-fuchsia-600 dark:text-fuchsia-300">Discover</p><h2 id="arena-coming-soon-heading" className="mt-1 text-2xl font-black text-slate-950 dark:text-white">Segera hadir</h2></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{games.map((game) => <div key={game.id} className="arena-coming-soon-card relative overflow-hidden rounded-2xl p-5 text-white"><div className={`absolute -right-5 -top-7 flex h-28 w-28 items-center justify-center rounded-[30px] bg-gradient-to-br ${game.gradient} rotate-12 opacity-80`}><game.icon size={38} /></div><div className="relative max-w-[68%]"><span className="inline-flex items-center gap-1 rounded-full bg-white/15 px-2.5 py-1 text-[10px] font-black uppercase tracking-wider"><Clock3 size={11} /> Segera hadir</span><h3 className="mt-5 text-lg font-black">{game.title}</h3><p className="mt-1 text-xs leading-relaxed text-violet-100">{game.description}</p><span className="mt-4 inline-flex items-center gap-1 text-[11px] font-bold text-amber-200"><Sparkles size={12} /> Dalam pengembangan</span></div></div>)}</div></section>; }
