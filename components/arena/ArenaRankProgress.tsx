import Link from "next/link";
import { ArrowUpRight, Crown } from "lucide-react";
import type { PlayerRank } from "@prisma/client";
import { RankIcon } from "@/components/gamification/RankIcon";
import { RANK_BANDS, RANK_META, nextRankOf } from "@/lib/gamification/ranks";

export default function ArenaRankProgress({ level, rank }: { level: number; rank: PlayerRank }) { const currentBand = RANK_BANDS.find((band) => band.rank === rank) ?? RANK_BANDS[0]; const nextRank = nextRankOf(rank); const position = Math.max(0, Math.min(1, (level - currentBand.min) / Math.max(1, currentBand.max - currentBand.min))); return <section className="arena-progress-surface rounded-[26px] p-5 sm:p-6"><p className="text-[11px] font-black uppercase tracking-[0.18em] text-violet-600 dark:text-violet-300">Progress</p><div className="mt-2 flex items-center justify-between gap-4"><h2 className="text-lg font-black text-slate-950 dark:text-white">Peringkatmu</h2><Crown className="text-amber-500" size={20} /></div><div className="mt-5 flex items-center gap-4"><RankIcon rank={rank} size={64} glow /><div><p className="text-lg font-black text-slate-900 dark:text-white">{RANK_META[rank].title}</p><p className="text-sm font-bold" style={{ color: RANK_META[rank].color }}>{RANK_META[rank].label} · Level {level}</p></div></div><div className="mt-5"><div className="flex justify-between text-[11px] font-bold text-slate-500 dark:text-slate-400"><span>Level {currentBand.min}</span><span>Level {currentBand.max}</span></div><div className="mt-2 h-2.5 overflow-hidden rounded-full bg-slate-200 dark:bg-white/10"><div className="h-full rounded-full" style={{ width: `${Math.round(position * 100)}%`, background: RANK_META[rank].color }} /></div></div><p className="mt-4 text-sm leading-relaxed text-slate-600 dark:text-slate-300">{nextRank ? <>Target berikutnya: <strong className="text-slate-900 dark:text-white">{RANK_META[nextRank].title}</strong> pada Level {RANK_META[nextRank].minLevel}.</> : "Kamu sudah mencapai rank tertinggi BahasaCerdas."}</p><Link href="/arena/player" className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-violet-700 dark:text-violet-300">Lihat perjalanan rank <ArrowUpRight size={15} /></Link></section>; }
