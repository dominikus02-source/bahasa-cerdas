import Link from "next/link";
import { CheckCircle2, ChevronRight, Flame, Target } from "lucide-react";
import { getQuestMeta, questProgressText } from "@/lib/quest-meta";
type Quest = { id: string; questType: string; target: number; progress: number; completed: boolean; rewardCoins: number };
export default function ArenaDailyTargets({ quests, streak }: { quests: Quest[]; streak: number }) { const completed = quests.filter((quest) => quest.completed).length; return <section className="arena-target-surface rounded-[26px] p-5 sm:p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-[11px] font-black uppercase tracking-[0.18em] text-emerald-700 dark:text-emerald-300">Achieve</p><h2 className="mt-1 text-xl font-black text-slate-950 dark:text-white">Target hari ini</h2></div><span className="inline-flex items-center gap-1 rounded-full bg-orange-100 px-2.5 py-1 text-xs font-black text-orange-700 dark:bg-orange-400/15 dark:text-orange-200"><Flame size={14} /> {streak}</span></div><div className="mt-4 flex items-center justify-between rounded-2xl bg-emerald-950 px-4 py-3 text-white"><span className="flex items-center gap-2 text-sm font-bold"><Target size={16} className="text-emerald-300" /> Misi selesai</span><span className="text-lg font-black">{completed}/{quests.length}</span></div><div className="mt-4 space-y-3">{quests.slice(0, 3).map((quest) => { const meta = getQuestMeta(quest.questType); const Icon = meta.Icon; const pct = quest.target ? Math.min(100, Math.round((quest.progress / quest.target) * 100)) : 0; return <div key={quest.id} className="flex items-center gap-3"><span className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-gradient-to-br ${meta.warna}`}>{quest.completed ? <CheckCircle2 size={18} className="text-white" /> : <Icon size={17} className="text-white" />}</span><div className="min-w-0 flex-1"><div className="flex justify-between gap-2"><p className="truncate text-sm font-bold text-slate-800 dark:text-slate-100">{meta.label}</p><span className="text-[10px] font-bold text-slate-500 dark:text-slate-400">{questProgressText(quest.progress, quest.target, quest.completed)}</span></div><div className="mt-1.5 h-1.5 overflow-hidden rounded-full bg-slate-200 dark:bg-white/10"><div className={`h-full rounded-full ${quest.completed ? "bg-emerald-500" : "bg-violet-500"}`} style={{ width: `${pct}%` }} /></div></div></div>; })}</div><Link href="/arena/misi" className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-emerald-700 hover:text-emerald-900 dark:text-emerald-300">Lihat semua misi <ChevronRight size={15} /></Link></section>; }
